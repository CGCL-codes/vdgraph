Too many datas in this rq3, so we only selcect deepwukong model to show.
We can not compare the similarity after differnt code obfuscation because the node id has changed.
So the 1/5/10 graph node Numbering method (ID) has been changed as same as 15.
The 0 graph node Numbering method (ID) has been changed as same as same 1.
So the results is different from rq1 and rq2.